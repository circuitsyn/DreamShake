<li>
	{{Shake}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{Drank}}">
		{{#if Drank}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li>

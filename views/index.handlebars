{{!-- Main Greeting Section --}}
<div class="container">
    <div class="jumbotron">
        <div class="col-md-6 bg mb-5">
            <h1 class="display-4 font-weight-bold">Get Your Shake On!</h1>
            <p class="lead font-weight-bold">Let's dream up your favorite kind of shake!</p>
        </div>
    
        <div class="col-md-12 bg font-weight-bold mt-5 pt-2 pb-1">
            <p>Get started by typing in your idea of an awesome shake below. Once you have submitted it you can then drink it by clicking devour. Let's get started!</p>
        </div>
    </div>

    {{!-- Submit Section --}}
    <div class="row mt-5" id="inputArea">
        <div class="col-md-12 d-flex justify-content-center">
            <form class="create-form">
                <div class="form-group mb-0 font-weight-bold">
                    <label for="ca">Name 'Dat Shake:</label>
                    <input type="text" id="ca" name="name">
                    <button type="submit" class="btn btn-danger">Create Drink!</button>
                </div>
                
            </form>
        </div>
    </div>
        

    {{!-- Content Section --}}
    <div  class="row d-flex justify-content-center">
        <div id="resultsArea" class="col-md-5 mr-2">
            
            <h2 class="resultTitle mb-0">Shake Creations</h2>
            <hr>
            <div class="row d-flex flex-row justify-content-center">
                <div id="shakeCreations" class="clearfix">
                    <ul>
                        {{#each shakes}}
                            {{#unless Drank}}
                            {{> shakes/shake-block Drank=true}}
                            {{/unless}}
                        {{/each}}
                    </ul>
                </div>
            </div>
        </div>

        

        <div id="resultsArea" class="col-md-5 ml-2">
            <h2 class="resultTitle mb-0">Devoured Shakes</h2>
            <hr>
            <div class="row flex-row justify-content-center">
                <div id="devouredShakes">
                    <ul>
                        {{#each shakes}}
                            {{#if Drank}}
                            {{> shakes/shake-block Drank=false}}
                            {{/if}}
                        {{/each}}
                    </ul>
                </div>
            </div>    
        </div>
    </div>

</div>